// const cdk = require('aws-cdk-lib');
// const { Template } = require('aws-cdk-lib/assertions');
// const NodejsServerlessTrainingCourse = require('../lib/nodejs-serverless-training-course-stack');

// example test. To run these tests, uncomment this file along with the
// example resource in lib/nodejs-serverless-training-course-stack.js
test('SQS Queue Created', () => {
//   const app = new cdk.App();
//   // WHEN
//   const stack = new NodejsServerlessTrainingCourse.NodejsServerlessTrainingCourseStack(app, 'MyTestStack');
//   // THEN
//   const template = Template.fromStack(stack);

//   template.hasResourceProperties('AWS::SQS::Queue', {
//     VisibilityTimeout: 300
//   });
});
